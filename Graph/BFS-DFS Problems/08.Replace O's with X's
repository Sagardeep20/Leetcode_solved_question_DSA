class Solution {
  public:
    void dfs(int row,int col,vector<vector<int>>& vis,vector<vector<char>>& grid,int delRow[],int delCol[]){
        int n = grid.size();
        int m = grid[0].size();
        vis[row][col] = 1;
        
        for(int i=0;i<4;i++){
            int nrow=row+delRow[i];
            int ncol=col+delCol[i];
            
            if(nrow>=0 && nrow<n && ncol>=0 && ncol<m && grid[nrow][ncol] == 'O' && vis[nrow][ncol] == 0){
                dfs(nrow,ncol,vis,grid,delRow,delCol);
            }
        }
    }
    void fill(vector<vector<char>>& grid) {
        // Code here
        int n = grid.size();
        int m = grid[0].size();
        vector<vector<int>>vis(n,vector<int>(m,0));
        
        int delRow[4] = {-1,0,1,0};
        int delCol[4] = {0,+1,0,-1};
        
        int i=0,j=0;//row0 
        while(j<m){
            if(grid[i][j] == 'O'){
                dfs(i,j,vis,grid,delRow,delCol);
            }
            j++;
        }
        i=n-1,j=0;
        while(j<m){
            if(grid[i][j] == 'O'){
                dfs(i,j,vis,grid,delRow,delCol);
            }
            j++;
        }
        j=0,i=0;
        while(i<n){
            if(grid[i][j] == 'O'){
                dfs(i,j,vis,grid,delRow,delCol);
            }
            i++;
        }
        i=0,j=m-1;
        while(i<n){
            if(grid[i][j] == 'O'){
                dfs(i,j,vis,grid,delRow,delCol);
            }
            i++;
        }
        
        for(int i=0;i<n;i++){
            for(int j=0;j<m;j++){
                if(vis[i][j] == 0){
                    grid[i][j] = 'X';
                }else{
                    grid[i][j] = 'O';
                }
            }
        }
        
    }
};
GFG - https://www.geeksforgeeks.org/problems/replace-os-with-xs0052/1
Leetcode - 130
